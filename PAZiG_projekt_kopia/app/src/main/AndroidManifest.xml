<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pazig_projekt">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".diary.AddToDiaryActivity"></activity>
        <activity android:name=".diary.DiaryActivity" />
        <activity android:name=".guide.AdvicesActivity" />
        <activity android:name=".food.NewHomemadeMealActivity" />
        <activity android:name=".products.ProductusActivity" />
        <activity android:name=".guide.GuideActivity" />
        <activity android:name=".dog.QuestionNotificaton" />
        <activity android:name=".dog.NewDogBodyConditionScore" />
        <activity android:name=".dog.NewDogNotifications" />
        <activity android:name=".food.ThisFoodActivity" />
        <activity android:name=".dog.MyDogActivity" />
        <activity android:name=".food.NewFoodActivity" />
        <activity
            android:name=".food.FoodList"
            android:theme="@style/AppTheme" />
        <activity android:name=".dog.NewDogActivity" />
        <activity android:name=".dog.DogListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".dog.notifications.AlertReceiver" />
    </application>

</manifest>